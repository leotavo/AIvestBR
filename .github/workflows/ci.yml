name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🛎️ Checkout do código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Puxa todas as tags para evitar problemas com bump2version

      - name: 🐍 Configurar Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest bump2version
          pip install -e .  # Instala o pacote em modo editável

      - name: ✅ Rodar testes
        env:
          PYTHONPATH: ${{ github.workspace }}/src  # Adiciona o diretório src ao PYTHONPATH
        run: |
          echo "🔎 Executando testes com pytest..."
          pytest tests/ || { echo "❌ Erro nos testes!"; exit 1; }

      - name: 🔖 Atualizar versão
        run: |
          set -e  # Faz o script falhar em qualquer erro

          git config --global user.email "github-actions@github.com"
          git config --global user.name "github-actions[bot]"

          echo "🔄 Sincronizando repositório..."
          git fetch --tags --force  # Garante que as tags locais estão atualizadas

          # Obtém a nova versão antes de aplicá-la
          NEW_VERSION=$(bump2version --dry-run --list patch | grep new_version | cut -d= -f2)
          echo "📢 Próxima versão detectada: v$NEW_VERSION"

          # Se a tag já existir, apaga a versão antiga para evitar erro
          if git rev-parse "v$NEW_VERSION" >/dev/null 2>&1; then
            echo "⚠️ A versão v$NEW_VERSION já existe. Deletando a tag antiga..."
            git tag -d "v$NEW_VERSION" || true  # Apaga a tag local
            git push --delete origin "v$NEW_VERSION" || true  # Apaga a tag remota
          fi

          echo "🚀 Atualizando para v$NEW_VERSION..."
          bump2version patch --verbose || { echo "❌ Erro no bump2version!"; exit 1; }

          echo "📤 Enviando alterações para o repositório..."
          git push origin main --follow-tags || { echo "❌ Erro ao enviar alterações!"; exit 1; }
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
